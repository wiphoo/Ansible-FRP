# SPDX-License-Identifier: MIT-0
### REQUIRED
# The namespace of the collection. This can be a company/brand/organization or product namespace under which all
# content lives. May only contain alphanumeric lowercase characters and underscores. Namespaces cannot start with
# underscores or numbers and cannot contain consecutive underscores
namespace: wiphoo

# The name of the collection. Has the same character restrictions as 'namespace'
name: frp

# The version of the collection. Must be compatible with semantic versioning
version: 0.2.0

# The path to the Markdown (.md) readme file. This path is relative to the root of the collection
readme: README.md

# A list of the collection's content authors. Can be just the name or in the format 'Full Name <email> (url)
# @nicks:irc/im.site#channel'
authors:
  - Wiphoo <me@wiphoo.dev>


### OPTIONAL but strongly recommended
# A short summary description of the collection
description: An Ansible collection for installing and managing Fast Reverse Proxy (frp) on Linux systems.

# Either a single license or a list of licenses for content inside of a collection. Ansible Galaxy currently only
# accepts L(SPDX,https://spdx.org/licenses/) licenses. This key is mutually exclusive with 'license_file'
license:
  - MIT

# A list of tags you want to associate with the collection for indexing/searching. A tag name has the same character
# requirements as 'namespace' and 'name'
tags: [frp, proxy, reverseproxy, networking, installation, security, hardening, compliance]

# Collections that this collection requires to be installed for it to be usable. The key of the dict is the
# collection label 'namespace.name'. The value is a version range
# L(specifiers,https://python-semanticversion.readthedocs.io/en/latest/#requirement-specification). Multiple version
# range specifiers can be set and are separated by ','
dependencies:
  # Required for UFW firewall configuration (community.general.ufw module)
  # Optional feature - role will gracefully handle if not available
  community.general: ">=11.0.0,<12.0.0"

# Note: Additional dependencies for Molecule testing (not required for role usage):
# - ansible.posix: ">=2.0.0,<3.0.0" (for Molecule test scenarios)
# - community.docker: ">=4.0.0,<5.0.0" (for Docker-based Molecule testing)
# These are specified in roles/frp_install/molecule/requirements.yml

# The URL of the originating SCM repository
repository: https://github.com/wiphoo/Ansible-FRP

# The URL to any online docs
documentation: https://wiphoo.github.io/Ansible-FRP/

# The URL to the homepage of the collection/project
homepage: https://wiphoo.dev

# The URL to the collection issue tracker
issues: https://github.com/wiphoo/Ansible-FRP/issues

# A list of file glob-like patterns used to filter any files or directories that should not be included in the build
# artifact. A pattern is matched from the relative path of the file or directory of the collection directory. This
# uses 'fnmatch' to match the files or directories. Some directories and files like 'galaxy.yml', '*.pyc', '*.retry',
# and '.git' are always filtered. Mutually exclusive with 'manifest'
build_ignore:
  # Virtual environments
  - .venv
  - .venv/
  - venv
  - venv/
  - env
  - env/

  # Ansible local cache and collections (at any level)
  - .ansible
  - .ansible/
  - "*/.ansible"
  - "*/.ansible/"
  - "**/.ansible"
  - "**/.ansible/"
  - collections
  - collections/
  - "*/collections"
  - "*/collections/"
  - "**/collections"
  - "**/collections/"

  # Python cache and build artifacts
  - .mypy_cache
  - .mypy_cache/
  - .pytest_cache
  - .pytest_cache/
  - .ruff_cache
  - .ruff_cache/
  - __pycache__
  - __pycache__/
  - "**/__pycache__"
  - "**/__pycache__/"
  - "*.pyc"
  - "*.pyo"
  - "*.pyd"
  - .coverage
  - .coverage.*
  - coverage.xml
  - htmlcov
  - htmlcov/
  - .tox
  - .tox/
  - .nox
  - .nox/
  - "*.egg-info"
  - "*.egg-info/"

  # Development and testing files
  - tests
  - tests/
  - test-results
  - test-results/
  - test-output
  - test-output/
  - scripts
  - scripts/

  # Documentation build files (keep docs/ for Galaxy display, exclude build configs)
  - mkdocs.yml
  - site/
  - site/**/*

  # Configuration files for development
  - pyproject.toml
  - uv.lock
  - codecov.yml
  - .python-version
  - .pre-commit-config.yaml
  - .ansible-lint
  - .yamllint
  - .gitignore
  - .nojekyll

  # IDE and editor files
  - .vscode
  - .vscode/
  - .idea
  - .idea/
  - "*.swp"
  - "*.swo"
  - "*~"

  # Operating system files
  - .DS_Store
  - Thumbs.db
  - ehthumbs.db
  - "._*"

  # Temporary and backup files
  - "*.retry"
  - "*.tmp"
  - "*.bak"
  - "*.backup"
  - .cache/
  - .tmp/

  # Logs
  - "*.log"
  - logs/

  # Molecule test files (keep only what's needed for users)
  - "roles/*/molecule/*/molecule.yml"
  - "roles/*/.molecule/"
  - "roles/**/.molecule/"

  # Build artifacts
  - "*.tar.gz"
  - wiphoo-frp-*.tar.gz
  - build/
  - dist/

# A dict controlling use of manifest directives used in building the collection artifact. The key 'directives' is a
# list of MANIFEST.in style
# L(directives,https://packaging.python.org/en/latest/guides/using-manifest-in/#manifest-in-commands). The key
# 'omit_default_directives' is a boolean that controls whether the default directives are used. Mutually exclusive
# with 'build_ignore'
# manifest: null
