---
# Molecule testing requirements for frp_install role (ci scenario)
collections:
  - name: community.general
    version: ">=7.0.0"

  - name: ansible.posix
    version: ">=1.0.0"

  - name: community.docker
    version: ">=3.0.0"
