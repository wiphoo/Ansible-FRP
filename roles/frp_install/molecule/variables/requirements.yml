---
# Molecule testing requirements for frp_install role (variables scenario)
collections:
  - name: community.general
    version: ">=11.0.0,<12.0.0"

  - name: ansible.posix
    version: ">=2.0.0,<3.0.0"

  - name: community.docker
    version: ">=4.0.0,<5.0.0"
