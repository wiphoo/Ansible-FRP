---
# Role-specific requirements for frp_install role
# These collections are required for the role to function properly
collections:
  - name: community.general
    version: ">=11.0.0,<12.0.0"
    # Used for:
    #   - UFW firewall configuration (community.general.ufw)
    # Note: Firewall configuration is optional and the role will gracefully
    # handle if this collection is not available
